@model ProjectManagr.ViewModels.ProjectSiteVM

@{
    Layout = null;
}

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            <h4 class="modal-title">Delete Project Site</h4>
           
        </div>

        <div class="modal-body">
            <h3>Are you sure you want to delete @Model.Name Project at @Model.SiteName ?</h3>
            @*@using (Ajax.BeginForm("Delete", "ProjectSite", null, new AjaxOptions { HttpMethod = "Post", OnSuccess = "OnSuccessfulProjectSiteDelete" }, new { @class = "form-horizontal", role = "form" }))
            {
                <div class="form-actions no-color">
                    @Html.HiddenFor(x => x.Id)
                    <input type="submit" value="Delete" class="btn btn-default" />
                </div>
            }*@
        </div>


        <div class="modal-footer">
            <div class="form-group">
            @using (Ajax.BeginForm("Delete", "ProjectSite", null, new AjaxOptions { HttpMethod = "Post", OnSuccess = "OnSuccessfulProjectSiteDelete" }, new { @class = "form-horizontal", role = "form" }))
            {
                <div class="col-md-offset-2 col-md-10">
                    @Html.HiddenFor(x => x.Id)
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <input type="submit" class="btn btn-danger" value="Delete" />
                </div>
                }
            </div>
        </div>
    </div>
</div>
